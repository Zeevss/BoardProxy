bin/
dist/
build/
# Каталог, в который CI складывает собранные артефакты.
artifacts/
node_modules/
*.tsbuildinfo

# Выхлоп `gomobile bind` (cd core && make mobile-aar). Собирается из core,
# поэтому в репозитории быстро расходится с ним: закоммиченный AAR отставал от
# кода и уносил с собой изменения, которых в нём просто не было.
/android/app/libs/boardproxy.aar
/android/app/libs/boardproxy-sources.aar
/android/app/libs/boardproxy-sources.jar
/control-plane/frontend/vite.config.js
/control-plane/frontend/vite.config.d.ts

.idea/
.vscode/
.claude/
.opencode/
.codex/

.env
# Server desired state contains private keys. Commit only the redacted example.
/config.toml
/core/config.toml
/subscribe/config.toml
/client.toml
/core/client.toml

*.log

.DS_Store
Thumbs.db

