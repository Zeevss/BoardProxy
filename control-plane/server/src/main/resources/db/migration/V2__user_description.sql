-- Описание пользователя: свободная пометка оператора («Design team · Москва»,
-- «Contractor»), которую панель показывает в строке списка рядом с именем.
--
-- Пустая строка вместо NULL: отличать «не заполнено» от «заполнено пустым»
-- здесь незачем, а NOT NULL избавляет чтение от постоянной развёртки null.
ALTER TABLE users ADD COLUMN description text NOT NULL DEFAULT '';
